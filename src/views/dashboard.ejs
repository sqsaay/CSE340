<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <h2>Dashboard</h2>
    <p>Name: <%= name %></p>
    <p>Username: <%= email %></p>

    <% if (user && user.role_name === 'admin') { %>
        <div class="page-actions" style="margin-top: 1rem;">
            <a href="/users">Manage Registered Users</a>
        </div>
    <% } %>

</main>

<%- include('partials/footer') %>